import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";const sql=readFileSync("supabase/migrations/202608140004_phase_2d_controlled_importer.sql","utf8");describe("Phase 2D controlled importer",()=>{it("verifies digests and idempotency",()=>{expect(sql).toContain("source digest mismatch");expect(sql).toContain("unique(organization_id,source_digest)")});it("uses an atomic rollback subtransaction",()=>{expect(sql).toContain("status='rolled_back'");expect(sql).toContain("exception when others")});it("keeps governance and financial prototype values inactive",()=>{expect(sql).toContain("'draft',false,'restricted'");expect(sql).not.toContain("estimated_value,import_batch_id")});it("never provisions auth identities",()=>expect(sql).not.toContain("auth.users"))});
