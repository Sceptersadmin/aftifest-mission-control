import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/202608140002_phase_2b_operational_core.sql","utf8");describe("Phase 2B operational core",()=>{it("defines provisional readiness without claiming an approved KPI",()=>{expect(sql).toContain("operational_readiness_provisional");expect(sql).toContain("avg(readiness)")});it("guards task management by assignment or department scope",()=>{expect(sql).toContain("can_manage_task");expect(sql).toContain("task.manage_assigned");expect(sql).toContain("department.read_all")});it("audits all operational mutation families",()=>{for(const table of ["departments_operational_audit","tasks_operational_audit","reports_operational_audit","comments_operational_audit"])expect(sql).toContain(table)});it("prevents cross-organization operational relationships",()=>{expect(sql).toContain("operational relationship crosses organization boundary")})});
